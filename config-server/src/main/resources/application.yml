
spring:
  cloud:
    config:
      server:
        git:
          uri:  file://Users/chaoqunshe/Developer/MicroService-config-repo
                    # uri: https://github.com/shechaoqun/MicroService-config-repo.git


server:
  port: 8020

---

spring:
  profiles: docker
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shechaoqun/MicroService-config-repo.git
