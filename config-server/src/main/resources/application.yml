spring:
  cloud:
    config:
      server:
        git:
          uri: file://${pwd}/../config-repo
server:
  port: 8020

spring:
  profiles: docker
  cloud:
      config:
        server:
          git:
            uri: https://

#spring:
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/spring-cloud-samples/config-repo
